# 1. Methods
```py
name = "a"
```
위 "a"라는 텍스트 안에는 많은 함수(사실은 메소드라고 함)들이 내장되어 있음.

## 1.1. function Method 차이
> 📌 function
>
> 독립적으로 존재하는 기능.

> 📌 Method
>
> data뒤에 결합이나 연결되어있는 기능.

---

# 2. List
리스트는 한 변수안에 여러 데이터(= value)를 넣을 수 있게 해줌.

```py
days_of_week = ["Mon","Tue","Wed","Thur","Fri"]
```
이런식으로 데이터를 한 변수안에 넣고, 메소드를 이용하면 다루기가 쉬워짐.

## 2.1. Methods
### 2.1.1. count
리스트 안에 특정 value가 몇개 있는지 알려줌.
```py
days_of_week = ["Mon","Tue","Wed","Thur","Fri"]
print(days_of_week.count("Mon"))

#출력
1
```
### 2.1.2. clear
list에 있는 모든 value들을 없앰.
```py
days_of_week = ["Mon","Tue","Wed","Thur","Fri"]
days_of_week.clear()
print(days_of_week)

#출력

```

### 2.1.3. reverse
list의 value들을 순서를 바꿔버림.
```py
days_of_week = ["Mon","Tue","Wed","Thur","Fri"]
days_of_week.reverse()
print(days_of_week)

#출력
["Fri","Thur","Wed","Tue","Mon"]
```

### 2.1.4. append
list에 value를 추가해줌.
```py
days_of_week = ["Mon","Tue","Wed","Thur","Fri"]
days_of_week.append("Sat")
days_of_week.append("Sun")

#출력
["Mon","Tue","Wed","Thur","Fri","Sat","Sun]
```

### 2.1.5. remove
list에 특정 value를 없앰.
```py
days_of_week = ["Mon","Tue","Wed","Thur","Fri"]
days_of_week.remove("Fri")

#출력
["Mon","Tue","Wed","Thur"]
```

## 2.2. 특정 value 접근
만약 days_of_week의 n번째에 접근하려면.
days_of_week[n-1]이라고 써주면 됨.

만약 3번째 값을 출력하고싶으면.
```py
days_of_week = ["Mon","Tue","Wed","Thur","Fri"]
print(days_of_week[2])

# 출력
Wed
```

## 2.3. 파이썬 리스트 특징
파이썬 리스트 value는 타입을 신경 쓰지 않아도 됨.
만약 int형 char 형 둘다 넣고싶다면 그냥 넣어버리면됨.

```py
l = [1,2,3,"hi","Hello",True,False]
print(l)

#출력
[1,2,3,"hi","Hello",True,False]
```

---

3. Dicts
사전을 예시로 들면.
사전에는 단어와 그의 정의가 있다. 그 단어와 정의가 키와 값이다.<br>
즉, 키는 단어이고 값은 정의다.

단어 - 정의 = Key - value(키-값)

## 3.1. 딕셔너리 생성 예시
```py
player = {
  'name' : '홍길동',
  'age' : 12,
  'alive' : True,
  'fav_food' : ["🍣", "🌮"]
}

print(player)

#출력
{'name' : '홍길동', 'age' : 12, 'alive' : True, 'fav_food : ["🍣", "🌮"]}
```

## 3.2. get
딕셔너리 값을 가져올 수 있게 해주는 메소드.<br>
키 값을 넣으면 됨.
```py
player = {
  'name' : '홍길동',
  'age' : 12,
  'alive' : True,
  'fav_food' : ["🍣", "🌮"]
}

print(player.get("name"))
print(player.get('fav_food'))

#출력
홍길동
["🍣", "🌮"]
```

근데 걍 '딕셔너리[키값]'이라고 쓰면 되긴함.

## 3.3. pop
딕셔너리 특정 값을 pop시킴.
```py
player = {
  'name' : '홍길동',
  'age' : 12,
  'alive' : True,
  'fav_food' : ["🍣", "🌮"]
}

print(player)
print(player.pop('age'))

#출력
{'name' : '홍길동', 'age' : 12, 'alive' : True, 'fav_food : ["🍣", "🌮"]}
12
{'name' : '홍길동', 'alive' : True, 'fav_food : ["🍣", "🌮"]}
```

## 3.4. append
값을 추가함.
```py
player = {
  'name' : '홍길동',
  'age' : 12,
  'alive' : True,
  'fav_food' : ["🍣", "🌮"]
}

print(player)
print(player['fav_food'[.append("🍕"))
print(player)

#출력
{'name' : '홍길동', 'age' : 12, 'alive' : True, 'fav_food : ["🍣", "🌮"]}
{'name' : '홍길동', 'age' : 12, 'alive' : True, 'fav_food : ["🍣", "🌮", "🍕"]}
```

---

# 3. Tuples
## 3.1. 튜플 선언 및 초기화
```py
days = ["Mon","Tue","Wed"] # 리스트
days = ("Mon","Tue","Wed") # 튜플
```

## 3.2. 튜플 출력
```py
days = ("Mon","Tue","Wed")

print(daya[0])
print(daya[-1])

# 출력
Mon
Wed
```

---

# 4. Dicts
딕셔너리는 key와 value 값을 가짐.

## 4.1 선언
```py
딕셔너리변수이름 = {
  key1:value1
  key2:value2
  ...
}
```

## 4.2 추가
```py
딕셔너리변수이름[새로운key] = 새로운value
```

## 4.3 value값 받기
```py
result = 딕셔너리변수이름.get(key)
```

---

# 5. For loops
for는 리스트 개수의 영향을 받아 반복하는 제어문.

```py
for 변수 in 리스트:
    내용
```
## 5.1. 사용예시
```python
websites = (
  "google.com",
  "airbnb.com",
  "twitter.com",
  "facebook.com"
)

for tomato in websites:
  print("hello!", tomato)
```

## 5.2. URL Formatting
```py
websites = (
  "google.com",
  "airbnb.com",
  "https://twitter.com",
  "facebook.com",
  "https://tiktok.com"
)
```
만약 이러한 튜플이 있다면, 진짜 링크를 어떻게 구별할까

### 5.2.1 startswith
파이썬엔 startswith라는 함수가 있음.

만약
```py
ans = 문자열1.startswith(문자열2)
```
이라고 쓰면 s가 문자열1이 문자열2로 시작하면 1을 반환하고, 아니면 0을 반환한다.

### 5.2.2 활용
```py
websites = (
    "google.com",
    "airbnb.com",
    "https://twitter.com",
    "facebook.com",
    "https://tiktok.com"
)

for site in websites:
    if site.startswith("https://"):
        print(f"'{site}' is good to go")
```

---

6. requests
https://pypi.org/ 에 가면 많은 라이브러리가 나옴.

그 중 쓸건 requests임.
웹사이트에게 요청을 보내면, 사이트가 웹사이트를 보내주는 라이브러리임.

## 6.2. 설치
```
pip3 install requests
```
## get
```py
import requests

변수 = requests.get(사이트 링크)
```

변수에 사이트 상태값을 반환함.

그런데 그냥 출력하면 이상한게(<Response [200]>) 출력됨.
그래서 get을 반환받은 변수뒤에 .status_code를 붙임.

그럼 잘 나옴.

## 6.3. 활용예시
```py
import requests

websites = (
    'https://google.com',
    'airbnb.com',
    'twitter.com' ,
    'https://facebook.com',
    'https://tiktok.com'
)

results = {}

for site in websites:
    if site.startswith("https://"):
        res = requests.get(site)
        
        if(res.status_code == 200):
            results[site] = "OK"
        else:
            results[site] = "FAILED"
        
print(results)
```
